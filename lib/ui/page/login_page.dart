/*
   DEVELOPED BY: KAMLESH LAKHANI

*/

import '../../import_package.dart';

class LoginPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) =>
      Scaffold(body: Center(child: Text('Login Page')));
}
